# Example agent config (clean)

This file exists so `tenken` has something to pass on.

## Setup

Run `npm run test` before you open a pull request.

## Where things live

The runner lives in `src/check.mjs`. Keep paths relative to the repository root.
