---
name: portable-thing
description: A minimal portable example skill, used to prove that tenken stays quiet on a clean tree. Reads one reference file and writes its output to a relative path.
---

# Portable thing

Read the template at `references/template.md` first.

Write the result to a relative path such as `./output/report.md`. Do not assume a
particular home directory: resolve everything from the repository root.
