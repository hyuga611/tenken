# Example agent config (intentionally broken)

This file exists so `tenken` has something to fail on. Every problem below is deliberate.

## Setup

Run `npm run nonexistent-script` before you start.

## Where things live

The parser lives in `src/this-file-does-not-exist.ts`.

Clone the repository to /Users/hyuga/dev/tenken and work from there.
